export const metadata = {
  title: "Insights",
  description:
    "Selected writing from Open Esquire on law, technology, Web3, and emerging markets.",
};

export default function BlogLayout({ children }) {
  return children;
}
